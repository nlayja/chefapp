<%= form_with(model: chef, local: true) do |form| %>
  <% if chef.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chef.errors.count, "error") %> prohibited this chef from being saved:</h2>

      <ul>
      <% chef.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nickname %>
    <%= form.text_field :nickname, id: :chef_nickname %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :chef_name %>
  </div>

  <div class="field">
    <%= form.label :class %>
    <%= form.text_field :class, id: :chef_class %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :chef_email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
